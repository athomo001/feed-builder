-r requirements.txt
pytest>=8
-r hub/requirements-dev.txt

# hub/ tiene su propio archivo de dependencias (hub/requirements.txt,
# hub/requirements-dev.txt) desde que Entrega 2 introdujo el Admin API
# (FastAPI/uvicorn/httpx), separado del script legado de arriba.
