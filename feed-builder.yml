# Respaldo/plantilla de los servicios del Hub (ver README.md seccion 9).
# Compose independiente y ejecutable:
#   docker compose -f feed-builder.yml up -d
# Sirve para recuperar rapidamente estos dos servicios si el compose
# principal (docker-compose.yml) se pierde o queda en mal estado --
# mantener sincronizado con el (mismas variables/defaults) cuando cambien
# variables, volumenes o command de hub-service/hub-api.
x-hub-common-env: &hub-common-env
  HUB_STATE_DIR: /state
  TXT_FEED_DIR: /feeds
  ALERT_SMTP_HOST: ${ALERT_SMTP_HOST:-}
  ALERT_SMTP_PORT: ${ALERT_SMTP_PORT:-25}
  ALERT_EMAIL_FROM: ${ALERT_EMAIL_FROM:-}
  ALERT_EMAIL_TO: ${ALERT_EMAIL_TO:-}
  ALERT_EMAIL_CREDENTIAL_REF: ${ALERT_EMAIL_CREDENTIAL_REF:-}
  ALERT_WEBHOOK_URL: ${ALERT_WEBHOOK_URL:-}
  ALERT_WEBHOOK_CREDENTIAL_REF: ${ALERT_WEBHOOK_CREDENTIAL_REF:-}
  SECRET_ENCRYPTION_KEY: ${SECRET_ENCRYPTION_KEY:-}
  OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}

services:
  hub-service:
    image: python:3.12-slim
    restart: always
    environment:
      <<: *hub-common-env
      HUB_SOURCE_ID: ${HUB_SOURCE_ID:-opencti-main}
      RECONCILE_INTERVAL_SECONDS: ${RECONCILE_INTERVAL_SECONDS:-600}
      ALERT_COOLDOWN_SECONDS: ${ALERT_COOLDOWN_SECONDS:-300}
    volumes:
      - ${HUB_APP_PATH:-.}:/app:ro
      - hub_state:/state
      - hub_feeds:/feeds
    working_dir: /app
    command: >
      sh -lc '
        set -eux;
        pip install --no-cache-dir -r hub/requirements.txt;
        exec python -m hub.service
      '
    healthcheck:
      test: ["CMD", "python", "-m", "hub.service", "--healthcheck"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - hub-net

  hub-api:
    image: python:3.12-slim
    restart: always
    depends_on:
      hub-service:
        condition: service_started
    environment:
      <<: *hub-common-env
      HUB_API_HOST: 0.0.0.0
      HUB_API_PORT: 8000
      ADMIN_UI_ORIGINS: ${ADMIN_UI_ORIGINS:-https://${PUBLIC_HOST}:${NGINX_HTTPS_PORT:-8446}}
      OIDC_ISSUER_URL: ${OIDC_ISSUER_URL:-}
      OIDC_CLIENT_ID: ${OIDC_CLIENT_ID:-}
      OIDC_CLIENT_SECRET_REF: ${OIDC_CLIENT_SECRET_REF:-}
      OIDC_REDIRECT_URI: ${OIDC_REDIRECT_URI:-}
      OIDC_ROLE_MAPPING: ${OIDC_ROLE_MAPPING:-}
    volumes:
      - ${HUB_APP_PATH:-.}:/app:ro
      - hub_state:/state
      - hub_feeds:/feeds
    working_dir: /app
    command: >
      sh -lc '
        set -eux;
        pip install --no-cache-dir -r hub/requirements.txt;
        exec python -m hub.api
      '
    healthcheck:
      # python:3.12-slim no trae curl/wget -- se usa urllib (ya en la
      # imagen base) contra el mismo endpoint que expone /healthz/ en nginx.
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/healthz/liveness', timeout=5)"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - hub-net

volumes:
  hub_state:
  hub_feeds:

networks:
  # Pre-creada a mano (`docker network create hub-net`) -- ver la misma
  # nota en docker-compose.yml sobre por que no se deja que Compose la
  # cree/borre en cada up/down.
  hub-net:
    external: true
